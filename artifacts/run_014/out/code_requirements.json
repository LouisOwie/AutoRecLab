"1. Load the MovieLens100k u.data file as four fields per row: user_id, item_id, rating, timestamp, using the correct delimiter and data types.\n2. Validate and report the total counts of unique user_ids and unique item_ids in the full file.\n3. Compute rating activity per user as the number of ratings each user made, rank users by this count descending, and return the top 3 most active users.\n4. For the single most active user, compute the mean of their rating values using all of their ratings.\n5. Identify item_id 50 and item_id 181 explicitly, then compute their co-occurrence as the number of distinct users who rated both items.\n6. Inspect all timestamps and compare them against the expected MovieLens 100k 1997-1998 date range; flag any timestamps outside the valid range and report how many/out which values.\n7. Ensure all computations are based on the full dataset, not samples, and handle ties or duplicates deterministically if they appear in activity counts.\n8. Produce outputs that are numerically exact and clearly labeled so each requested result can be directly reported back to the user."